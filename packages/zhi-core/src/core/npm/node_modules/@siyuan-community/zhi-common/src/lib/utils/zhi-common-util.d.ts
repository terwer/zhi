/**
 * 简单的日志接口
 */
interface ILogger {
    debug: (msg: string, obj?: any) => void;
    info: (msg: string, obj?: any) => void;
    error: (msg: string | Error, obj?: any) => void;
}
/**
 * 为了减少依赖，这里写了一个简单的日志工具，仅在 common 使用
 *
 * @author terwer
 * @version 0.1.0
 * @since 0.1.0
 */
export declare const createCommonLogger: (name: string) => ILogger;
export {};
